# SpamEmailClassifier
Built a Spam Email Classifier using NLP and Machine Learning to classify messages as spam or not spam. The project uses text preprocessing, TF-IDF vectorization, and Multinomial Naive Bayes, achieving around 97% accuracy. It also includes model evaluation using precision, recall, F1-score, and a Streamlit app for easy prediction.
